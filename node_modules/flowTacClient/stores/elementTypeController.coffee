utils = require("flowtacCommon/utilities")

class @ElementTypeController
  constructor:(options)->
    @app = @getOption(options.app, null)

  getOption:(attr, dflt)->
    utils.getOptionValue(attr, dflt)

